setupRequired(hscPipe >= 3.3.3)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
envPrepend(PATH, ${PRODUCT_DIR}/bin)

# user bin/ directories
envPrepend(PATH, ${PRODUCT_DIR}/bick/bin)
envPrepend(PATH, ${PRODUCT_DIR}/clackner/bin)
envPrepend(PATH, ${PRODUCT_DIR}/song/bin)
envPrepend(PATH, ${PRODUCT_DIR}/surhud/bin)

